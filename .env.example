# Local development defaults (copy to .env.local if needed).
# Production Docker builds pass these via Dockerfile ARG / build-and-push.ps1.

# Canonical public site URL (SEO, Open Graph, sitemap, structured data)
VITE_SITE_URL=http://localhost:5173

# Optional Google Search Console verification meta tag
# VITE_GOOGLE_SITE_VERIFICATION=
